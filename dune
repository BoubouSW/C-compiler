(ocamllex
 (modules lexer))
(menhir
 (modules parser))
(library
  (name reader)
  (modules lexer parser ast_c ast_mips compiler reader)
  (libraries unix)
)
(executable
 (name ptigcc)
 (promote (until-clean))
 (modules ptigcc)
 (libraries unix reader)
 (flags ())
)
